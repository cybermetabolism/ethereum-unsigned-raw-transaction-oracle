### Simple API App in Python

Built using flask and the flask-restful library.

* Automated testing set up with tavern.yaml


#### Installation

Ensure python3 is installed

```py
pip install -r requirements.txt
pip install tavern[pytest]
```